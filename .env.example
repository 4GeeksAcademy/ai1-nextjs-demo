# Data provider: sqlite (default local) or postgres (Neon/Vercel)
DATA_PROVIDER=sqlite

# Neon/Vercel Postgres connection string (preferred when DATA_PROVIDER=postgres)
POSTGRES_URL=

# Fallback Postgres connection string (used if POSTGRES_URL is not set)
DATABASE_URL=
